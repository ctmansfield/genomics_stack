# Patch Plan: clinician-top20-system-coverage

Patch name
- clinician-top20-system-coverage

Problem statement
- Clinicians need concise Top 20 findings with biochemical explanations and balanced representation across physiological systems. Current outputs are technical and may cluster in a single system.

Objectives
- Add system tagging to rules, apply system coverage in Top 20 selection, and render clinician-grade HTML/PDF.

Work breakdown
1) Schema: add risk_rules.system_tag (default 'General'); migration SQL and idempotent checks.
2) Docs: systems taxonomy; deliverables/ACs doc (this file) + change log entry.
3) Selection module: query pair-aware candidates and enforce coverage (Python script in scripts/reports/clinician_top20.py).
4) HTML/PDF rendering: clinician layout with explanations and system headers.
5) Verification harness: deterministic run commands; sample outputs.

Milestones and deliverables
- M1 (Schema):
  - SQL: scripts/sql/risk_panel.sql updated or new migration sql/20251005_system_tag.sql
  - AC: column exists; default applied; existing rows readable.
- M2 (Docs):
  - docs/clinician_report/SYSTEMS_TAXONOMY.md
  - docs/changes/20251005_120000_clinician-top20-system-coverage.md
  - CHANGELOG.md entry
- M3 (Selection):
  - scripts/reports/clinician_top20.py (fetch + coverage selection)
  - AC: ensures ≥1 per present system; 20 rows max
- M4 (Rendering):
  - HTML and PDF outputs, embedded disclaimers; tables with RSID/Gene/Title/Zygosity/Impact/Explanation/Notes/Evidence.
- M5 (Verification):
  - Commands and acceptance checks pass on two uploads or simulated data.

Acceptance criteria (summary)
- See docs/changes/20251005_120000_clinician-top20-system-coverage.md (AC1–AC6).

Risks
- Inconsistent envs; mitigate with DSN support and docker compose exec path.
- Limited curation; mitigate with defaults and taxonomy guidance.

Backout
- Non-breaking additive changes; renderer can ignore system_tag.
